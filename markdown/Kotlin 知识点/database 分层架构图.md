```text
┌───────────────────────────────┐
│ UI 层                          │
│ Compose / Activity / Fragment │
│ - 展示数据                      │
│ - 触发用户操作                  │
└───────────────▲───────────────┘
                │
                │ 调用
                │
┌───────────────┴───────────────┐
│ ViewModel 层                   │
│ - 暴露 StateFlow / LiveData    │
│ - 调用 Repository              │
│ - 处理业务逻辑                  │
└───────────────▲───────────────┘
                │
                │ 调用
                │
┌───────────────┴───────────────┐
│ Repository 层                  │
│ - 统一数据来源（本地/网络）        │
│ - 调用 DAO                     │
│ - 组合数据、封装结果              │
└───────────────▲───────────────┘
                │
                │ 获取 DAO
                │
┌───────────────┴───────────────┐
│ Database 层                    │
│ @Database                      │
│ - 提供 DAO 实例                 │
│ - 管理数据库连接                 │
└───────────────▲───────────────┘
                │
                │ 引用 Entity
                │
┌───────────────┴───────────────┐
│ DAO 层                         │
│ @Dao                          │
│ - 定义 CRUD                    │
│ - 依赖 Entity                  │
└───────────────▲───────────────┘
                │
                │ 定义表结构
                │
┌───────────────┴───────────────┐
│ Entity 层                     │
│ @Entity                       │
│ - 表结构                       │
│ - 字段、主键、索引               │
└───────────────────────────────┘
```
